import { DemensionsPipe } from './demensions.pipe';

describe('DemensionsPipe', () => {
  it('create an instance', () => {
    const pipe = new DemensionsPipe();
    expect(pipe).toBeTruthy();
  });
});
